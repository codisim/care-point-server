generator client {
  provider = "prisma-client"
  output   = "../../src/generated"
  // output   = "./src/generated/client"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
